server:
  port: 8085
spring:
  application:
    name: TAGS
  cloud:
    consul:
      host: 192.168.1.30
      port: 8500
      discovery:
        heartbeat:
          enabled: true
        service-name: ${spring.application.name}